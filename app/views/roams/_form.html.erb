<%= form_for(@roam) do |f| %>
  <% if @roam.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@roam.errors.count, "error") %> prohibited this roam from being saved:</h2>

      <ul>
      <% @roam.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
